import { ILabelText } from "./label-text.interface";
import { ITextInput } from "./text-input.interface";

export interface IInputField  {
  labelProps: ILabelText;
  textInputProps: ITextInput;
}