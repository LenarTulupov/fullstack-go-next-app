import { useDispatch, useSelector } from "react-redux"

export default function useSizeChartPopup() {
  const dispach = useDispatch();

  const isSizeChartPopupOpened = useSelector((state) => state.)
  return 
};
